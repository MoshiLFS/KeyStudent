import React from 'react';
import style from './index.less'

const Header = () => {
  return (
    <div className={style.header}>
        重点学生日周考成绩录入管理系统
    </div>
  );
};

Header.propTypes = {
};

export default Header;
